{
  "compilerOptions": {
    /* Visit https://aka.ms/tsconfig to read more about this file */
    /* Language and Environment */
    "target": "ES3",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "lib": ["DOM","ES2015"],                                        /* Specify a set of bundled library declaration files that describe the target runtime environment. */
    "jsx": "react-jsx",                                /* Specify what JSX code is generated. */
    "jsxImportSource": "hono/jsx",                            /* Specify module specifier used to import the JSX factory functions when using 'jsx: react-jsx*'. */

    /* Modules */
    "module": "AMD",                                /* Specify what module code is generated. */
    "moduleResolution": "Node",
    "rootDirs": ["./hanabi", "./source"],                                   /* Allow multiple folders to be treated as one when resolving modules. */
    "types": ["node", "./global.d.ts", "hono"],                                      /* Specify type package names to be included without being referenced in a source file. */

   "baseUrl": "./", // Ruta base de tus módulos en TypeScript
    "paths": {
      "hono/jsx": ["hanabi/jsx"],
      "source/*": ["source/*"],
      "hanabi": ["hanabi"]
    },
    /* Emit */
    "sourceMap": true,                                /* Create source map files for emitted JavaScript files. */
    "outDir": "./public/js",                                   /* Specify an output folder for all emitted files. */
    "esModuleInterop": true,                             /* Emit additional JavaScript to ease support for importing CommonJS modules. This enables 'allowSyntheticDefaultImports' for type compatibility. */
    "forceConsistentCasingInFileNames": true,            /* Ensure that casing is correct in imports. */

    /* Type Checking */
    //"composite": true,   /* Activa la compilación incremental y almacena información en un directorio específico. */
    "strict": true,                                      /* Enable all strict type-checking options. */
    "skipLibCheck": true,                                /* Skip type checking all .d.ts files. */
    "ignoreDeprecations": "5.0"
  }
}
